# Generated by scripts/gen-controller.sh from deploy/crds. Do not edit.
{{- if .Values.crds.install }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    helm.sh/resource-policy: keep
    controller-gen.kubebuilder.io/version: v0.22.0
  name: clusteregresspolicies.g0efilter.g0lab.com
spec:
  group: g0efilter.g0lab.com
  names:
    categories:
    - g0efilter
    kind: ClusterEgressPolicy
    listKind: ClusterEgressPolicyList
    plural: clusteregresspolicies
    shortNames:
    - g0cep
    singular: clusteregresspolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ClusterEgressPolicy is a cluster-scoped set of egress rules merged into the
          namespaced policies it selects.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ClusterEgressPolicySpec describes egress allowed across namespaces.

              Cluster policies are additive: their rules are merged into every EgressPolicy in
              a matching namespace. They cannot remove anything a namespaced policy allows, so a
              platform team uses them for baseline destinations such as cluster DNS rather than
              as a restriction on tenants.
            properties:
              egress:
                description: Egress lists the allowed destinations.
                items:
                  description: EgressRule allows traffic to a set of peers, optionally
                    narrowed to ports.
                  properties:
                    name:
                      description: Name is an optional label for the rule, used in
                        status and events.
                      maxLength: 63
                      type: string
                    ports:
                      description: |-
                        Ports narrows the rule to specific protocols and ports. When empty, every
                        port is allowed to the listed peers.
                      items:
                        description: EgressPort narrows a rule to a protocol and port.
                        properties:
                          port:
                            description: Port is the destination port.
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          protocol:
                            default: TCP
                            description: Protocol is TCP or UDP.
                            enum:
                            - TCP
                            - UDP
                            type: string
                        required:
                        - port
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    to:
                      description: To lists the destinations this rule allows. At
                        least one is required.
                      items:
                        description: EgressPeer is a set of domain or network destinations.
                        properties:
                          domainNames:
                            description: |-
                              DomainNames matches by HTTP Host or TLS SNI. Supports an exact name, a
                              leading wildcard such as `*.example.com`, or a `/regex/` pattern.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                          networks:
                            description: Networks matches by IP or CIDR, for destinations
                              with no usable domain name.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: atomic
                  required:
                  - to
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              namespaceSelector:
                description: |-
                  NamespaceSelector selects the namespaces this policy is merged into. An empty
                  selector matches every namespace.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
{{- end }}
