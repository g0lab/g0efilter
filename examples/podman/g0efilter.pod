[Unit]
Description=g0efilter egress-filtering pod

# A pod shares one network namespace between its containers, which is what lets
# g0efilter filter the workload beside it - the same model as a Kubernetes pod.
[Pod]

[Install]
WantedBy=default.target
