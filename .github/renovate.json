{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":semanticCommits"
  ],
  "timezone": "Australia/Sydney",
  "separateMajorMinor": true,
  "pinDigests": true,
  "platformCommit": "enabled",
  "platformAutomerge": true,
  "commitBodyTable": true,
  "postUpdateOptions": ["gomodTidy"],
  "prHourlyLimit": 0,
  "prConcurrentLimit": 0,
  "branchConcurrentLimit": 0,
  "description": [
    "ignorePaths restates :ignoreModulesAndTests without **/examples/**, so the example Containerfiles and manifests track the same versions the shipped images do. The field is not mergeable, so dropping one entry means listing them all."
  ],
  "ignorePaths": [
    "**/node_modules/**",
    "**/bower_components/**",
    "**/vendor/**",
    "**/__tests__/**",
    "**/test/**",
    "**/__fixtures__/**"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "The setup-go pins are the release toolchain.",
      "managerFilePatterns": ["/^\\.github/workflows/.+\\.yaml$/"],
      "matchStrings": ["go-version:\\s*'(?<currentValue>\\d+\\.\\d+\\.\\d+)'"],
      "depNameTemplate": "go",
      "datasourceTemplate": "golang-version"
    }
  ],
  "packageRules": [
    {
      "description": "config:recommended pulls in :semanticPrefixFixDepsChoreOthers, which types production dependencies as fix and everything else as chore. Release notes then split identical dependency bumps across Fixed and Changed, so type them all as chore.",
      "matchPackageNames": ["*"],
      "semanticCommitType": "chore"
    },
    {
      "description": "Auto-merge non-major updates once they have aged a day. GitHub's own auto-merge holds each PR until the required checks pass, so groups land independently rather than one per run.",
      "matchUpdateTypes": ["minor", "patch"],
      "minimumReleaseAge": "1 day",
      "automerge": true,
      "automergeType": "pr"
    },
    {
      "description": "A digest bump is the same tag rebuilt, usually for a CVE patch, and carries no release notes to age. Soaking it would also never converge: each rebuild restarts the clock.",
      "matchUpdateTypes": ["digest", "pin", "pinDigest"],
      "minimumReleaseAge": "0",
      "automerge": true,
      "automergeType": "pr"
    },
    {
      "description": "One PR per ecosystem, as Dependabot grouped them. Majors stay separate so a breaking change is never bundled with an automerged bump.",
      "matchUpdateTypes": ["minor", "patch"],
      "matchManagers": ["gomod"],
      "groupName": "gomod"
    },
    {
      "matchUpdateTypes": ["minor", "patch"],
      "matchManagers": ["npm"],
      "groupName": "npm"
    },
    {
      "matchUpdateTypes": ["minor", "patch"],
      "matchManagers": ["github-actions"],
      "groupName": "github-actions"
    },
    {
      "matchUpdateTypes": ["minor", "patch"],
      "matchManagers": ["dockerfile", "docker-compose"],
      "groupName": "docker"
    },
    {
      "matchUpdateTypes": ["minor", "patch"],
      "matchManagers": ["helmv3", "kustomize"],
      "groupName": "kubernetes manifests"
    },
    {
      "description": "Same-version updates get their own branch rather than joining an ecosystem group. A digest or pin leaves newValue unchanged, so grouping one alongside a version bump for the same dependency gives the branch two targets and Renovate discards the collision. Split per manager so one dependency Renovate cannot rewrite does not abandon every other digest.",
      "matchUpdateTypes": ["digest", "pin", "pinDigest"],
      "groupName": "{{manager}} digests"
    },
    {
      "description": "A major is a breaking change: its own PR, ungrouped, reviewed by a human.",
      "matchUpdateTypes": ["major"],
      "automerge": false
    },
    {
      "description": "Never offer pre-release container tags.",
      "matchDatasources": ["docker"],
      "allowedVersions": "!/-(alpha|beta|rc|pre|preview)/"
    },
    {
      "description": "No workflow tests .devcontainer, so these land as a PR for a human rather than automerging unchecked.",
      "matchManagers": ["devcontainer"],
      "groupName": "devcontainer",
      "automerge": false
    },
    {
      "description": "scripts/set-version.sh owns every g0efilter pin and tests/repo asserts they equal VERSION, so Renovate must not move them.",
      "matchPackageNames": ["/g0efilter/"],
      "enabled": false
    },
    {
      "description": "set-version.sh rewrites deploy, docs, examples, controller and the README, never .github, so the workflows that run g0efilter on itself are the one g0efilter pin Renovate has to own. Scoped to the manager rather than loosening the rule above, which still has to hold for the Kustomize ?ref= pins.",
      "matchManagers": ["github-actions"],
      "matchPackageNames": ["/g0efilter/"],
      "enabled": true
    },
    {
      "description": "The go directive is the language floor, kept below the release toolchain on purpose so the modules still build with an older Go.",
      "matchManagers": ["gomod"],
      "matchDatasources": ["golang-version"],
      "enabled": false
    },
    {
      "description": "The release toolchain lives in the workflow setup-go pins and the E2E builder images. Group them so one PR moves every copy; tests/repo fails a bump that misses one.",
      "matchPackageNames": ["go", "golang"],
      "matchDatasources": ["golang-version", "docker"],
      "groupName": "go toolchain"
    },
    {
      "description": "curl and k3s are throwaway test and demo peers, wanted at whatever latest is rather than at a recorded version. Un-ignoring examples/ would otherwise start pinning them.",
      "matchPackageNames": ["/curl/", "/k3s/"],
      "enabled": false
    },
    {
      "description": "workarounds:all teaches Renovate that chart-testing-action's version input tracks the helm/chart-testing CLI, so the dep resolves against that repo while the only text to rewrite is the action's uses line. A digest from the CLI repo does not exist in the action repo, so pinning one can never be applied and fails the whole branch.",
      "matchDepNames": ["chart-testing"],
      "pinDigests": false
    }
  ],
  "lockFileMaintenance": {
    "enabled": true,
    "automerge": true,
    "automergeType": "pr"
  }
}
