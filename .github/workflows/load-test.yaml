---
name: Load Test

# Scaled-up custom stress run. Normal PR e2e already runs tests/e2e/12_load.sh.
on:
  workflow_dispatch:
    inputs:
      mode:
        description: 'filter mode'
        type: choice
        options: [https, dns]
        default: https
      total:
        description: 'total blocked connections to fire'
        default: '2000'
      concurrency:
        description: 'concurrent connections per wave'
        default: '100'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  load:
    name: ${{ inputs.mode }}
    runs-on: ubuntu-latest
    steps:
      - name: Filter egress
        uses: g0lab/g0efilter@55823a90269e8e00308ee78b27cef2e3dd9137a8 # v0.9.9
        with:
          egress-policy: audit

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Set up Docker Compose
        uses: docker/setup-compose-action@54042514f505b273907334ae2b9cdbb9a0213c1a # v2

      - name: Run load test
        env:
          FILTER_MODE: ${{ inputs.mode }}
          LOAD_TOTAL: ${{ inputs.total }}
          LOAD_CONCURRENCY: ${{ inputs.concurrency }}
        run: scripts/load.sh
