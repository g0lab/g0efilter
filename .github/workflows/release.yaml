---
name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    environment: dockerhub
    timeout-minutes: 30
    permissions:
      contents: write  # Publish the release.
      id-token: write  # Sign artifacts with Cosign.
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Tag points to main
        run: |
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "::error::release tags must point to a commit on main"
            exit 1
          fi

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version: '1.26'
          cache: true

      # The tag is authoritative. VERSION mirrors it so a checkout of the tag has
      # manifests and docs pinned to that same release. Checked here as well as in
      # CI because a tag can be pushed at a commit whose CI never ran.
      - name: Version is consistent
        run: |
          declared="v$(tr -d '[:space:]' < VERSION)"
          if [ "$declared" != "${GITHUB_REF_NAME}" ]; then
            echo "::error::VERSION is $declared but the tag is ${GITHUB_REF_NAME};"\
                 "run scripts/set-version.sh ${GITHUB_REF_NAME} and commit before tagging"
            exit 1
          fi
          go test -count=1 ./tests/repo/ -run 'TestPinnedVersions|TestEveryExpectedFile'

      # GoReleaser builds the dashboard UI in its before hook.
      - name: Set up pnpm
        uses: pnpm/action-setup@v6
        with:
          package_json_file: dashboard/ui/package.json

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: pnpm
          cache-dependency-path: dashboard/ui/pnpm-lock.yaml

      - name: Check Go modules
        run: |
          go mod tidy
          git diff --exit-code

      - name: Vet
        run: go vet ./...

      - name: Build dashboard UI
        run: |
          pnpm --dir dashboard/ui install --frozen-lockfile
          pnpm --dir dashboard/ui build

      - name: Test
        run: go test -race -covermode=atomic -coverprofile=coverage.txt ./...

      - name: Lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest
          args: >
            --timeout=10m
            ./...

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Install Cosign
        uses: sigstore/cosign-installer@v3

      - name: Release
        uses: goreleaser/goreleaser-action@v7
        with:
          distribution: goreleaser
          version: '~> v2'
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  update-major-tag:
    name: Move major version tag
    needs: release
    runs-on: ubuntu-latest
    # Do not move the rolling tag for prereleases.
    if: ${{ !contains(github.ref_name, '-') }}
    permissions:
      contents: write  # Move the rolling major tag.
    steps:
      - uses: actions/checkout@v7

      - name: Move major tag
        run: |
          MAJOR="${GITHUB_REF_NAME%%.*}"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -f "$MAJOR" "$GITHUB_REF_NAME"
          git push origin "$MAJOR" --force
